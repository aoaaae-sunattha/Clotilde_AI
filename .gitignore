.env
node_modules/

# QA test runtime artifacts — regenerated on every run
QA/tests/.last_notify_output.json
QA/results/02_automation_raw/

# Playwright auth state (would contain credentials)
QA/tests/fixtures/.auth*.json

# Runtime data files — contain live booking/chat state
bookings.json
chat_registry.json
approvals.json

# Cypress generated reports (uploaded as CI artifacts, not committed)
cypress/reports/

# macOS
.DS_Store
.gstack/
